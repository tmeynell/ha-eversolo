# Test harness for the mocked-HTTP seam. Pinned to the phcc release that actually requires
# homeassistant==2026.8.2 (its own METADATA's Requires-Dist, not just a nearby version) — giving
# aioclient_mock, the hass fixture, and enable_custom_integrations. requirements.txt is pinned to
# match; earlier comments here claimed both matched 2026.4.0, which was never true for this phcc
# release and only surfaced when the dev venv actually installed exactly what was pinned.
pytest-homeassistant-custom-component==0.13.356

# Matches manifest.json's requirements pin — HA installs this itself in a real
# deployment, but the test environment needs its own copy since coordinator.py
# imports wake_on_lan.py transitively.
wakeonlan==4.0.0

# Matches the bundled homeassistant==2026.8.2's own pin for the ssdp
# integration (components/ssdp/manifest.json) — HA installs this itself in a
# real deployment, but the test environment needs its own copy since
# config_flow.py now imports homeassistant.components.ssdp directly (#27).
async-upnp-client==0.47.1

# Matches manifest.json's requirements pin — HA installs this itself in a real
# deployment, but the test environment needs its own copy since camera.py
# imports av directly to decode the cast-mode socket's H.264 (#38).
av==16.0.1

# Not in manifest.json — homeassistant.components.camera.img_util imports this
# unconditionally at module level (no try/except), so it has to be installed
# for tests to even collect test_camera.py, the same way async-upnp-client
# above stands in for what a real deployment's own camera platform installs.
PyTurboJPEG==1.8.0
